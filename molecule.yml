---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: ansible-role-influxdb
      image: centos
      image_version: centos7.3.1611
      ansible_groups:
        - group1
verifier:
  name: testinfra

molecule:
  ignore_paths:
    - venv
